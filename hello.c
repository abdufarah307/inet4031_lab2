#include <stdio.h>

int main() {
    // Creating an array of strings in C
    const char *users[] = {"User1", "User2", "User2"};

    int a = 2;
    int b = 2;
    int c = a + b;

    printf("C says: Hello, World!\n");
    printf("%d + %d = %d\n", a, b, c);

    return 0;
}
